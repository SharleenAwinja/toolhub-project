import React from 'react'
import Background from '../components/Background';

const Home = ({toolsInfo}) => {
  return (
    <div>
      <Background toolsInfo={toolsInfo}/>
    </div>
  )
};

export default Home;
